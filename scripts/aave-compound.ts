import { WALLET_ADDRESS, ADDRESSES } from "../constants";
import { LendingPoolInterfaceV2 } from "@aave/protocol-js";

async function main() {}

main().catch((error) => {
  console.error(error);
  process.exitCode = 1;
});
